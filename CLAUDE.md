# Legacy Agent Instructions

This file is kept only for tools that still look for `CLAUDE.md`.

Authoritative repo-wide rules are in `AGENTS.md`.

UI-specific rules remain in `lib/presentation/CLAUDE.md`.

Do not add new repo-wide rules here. Update `AGENTS.md` instead.
